package com.bibliotheque.gestion_pret.enums;

public enum TypeValeurParametre {
    string,
    integer,
    decimal,
    BOOLEAN, // Note: BOOLEAN is not a typo, it is used to represent boolean values
    date
}